
## 项目概览

这是一个专注于 **AI 系统架构与 Agent 工程** 的个人知识库。
关注领域：系统架构设计、Agent 安全、记忆管理、Skill 迭代机制、上下文工程等等。
此仓库的 `wiki/` 完全由 Claude 负责编写和维护，用户只提供原始资料和问题。

## 关键的三层架构

### 第一层: `raw/` (原始资料 - 只读)
- **目的**: 存放不同方向的技术报告。
- **规则**: **严禁修改或删除** `raw/` 中的任何内容。这是证据来源。

### 第二层: `wiki/` (编译产物 - 完全由 AI 维护)
- **目的**: 编译后的架构知识图谱。Claude 负责编写和维护。
- **结构**: 纯 Markdown 文件。使用 `[[双向链接]]` 关联组件、模式与漏洞。
- **每个模块目录下，页面分两类**:
    - **开源框架分析页**（命名如 `hermes-xxx.md`、`deerflow-xxx.md`、`openclaw-xxx.md`）：记录 Hermes / OpenClaw / DeerFlow 各自的技术方案，原汁原味，不掺杂中台设计。
    - **中台设计页**（命名如 `xxx.md`，无框架前缀）：基于开源框架的参考与对比，输出我们自己的 AI 中台设计方案，明确写出"参考了哪个框架的哪个设计"。
    - 两类页面通过 `[[双向链接]]` 关联，中台设计页应注明参考来源。

### 第三层: `CLAUDE.md` (规则文件 - 本文件)
- **目的**: 约定 Claude 的行为规范、工作流程和 wiki 写作标准。
- **规则**: 用户可修改，Claude 严格遵守，不得自行篡改。

## 具体工作流指令

### 1. 收录新资料
当用户说"帮我收录"、"处理一下"、"把这个加进去"等类似意思时，执行以下步骤：
- **找到新文件**: 运行 `git status --short raw/` 和 `git diff --name-only --diff-filter=A HEAD raw/`，找出 `raw/` 下**未跟踪 (`??`) 或新增 (`A`) 的文件**。没有新文件就告诉用户，不要继续。
- **读懂内容**: 仔细阅读，提炼出涉及的**架构模式**、**安全边界**、**记忆机制**或**上下文策略**等核心要点。
- **找准归属位置**:
    1. 先看 `wiki/` 现有目录，判断内容往哪个模块放（`architecture/`、`security/`、`memory/`、`context/` 等）。
    2. 没有合适的模块，就在 `wiki/` 下新建子目录，同时在 `wiki/INDEX.md` 里登记新模块。
    3. **判断文件类型**：
       - 内容是 Hermes / OpenClaw / DeerFlow 的源码分析或技术报告 → 写入或更新**开源框架分析页**（如 `hermes-memory.md`）。
       - 内容是我们自己的设计方案（署名"大迈"，标题含"AI 中台"、"企业级"等）→ 写入或更新**中台设计页**（如 `memory-management.md`），并在文中注明参考了哪个框架的哪个设计。
- **逐一更新**:
    1. 更新 `wiki/architecture/` 里的架构模式页。
    2. 更新 `wiki/security/` 里的安全攻击面或防御措施页。
    3. 更新 `wiki/memory/` 或 `wiki/context/` 的相关页面。
    4. 更新 `wiki/INDEX.md` 和 `wiki/log.md`。

### 2. 文档补齐
当用户说"文档不够全面"、"列出功能清单"、"先列清单再补齐"、"哪些地方没写到"等类似意思时，执行以下步骤：
- **生成功能清单**: 对照对应的 `raw/` 原始文件，逐项列出该主题**应该覆盖的所有功能/机制**（用表格或列表，标注已覆盖 ✅ / 缺失 ❌）。
- **确认后补齐**: 把清单给用户看，确认范围后逐项补写缺失内容，更新对应 wiki 页面。
- **框架对比文档**: 如果补齐过程中涉及多个框架的横向比较，同步更新或新建 `wiki/comparison.md`，并登记到 `wiki/INDEX.md`。

### 3. 框架对比
当用户说"对比一下"、"不同框架的区别"、"横向比较"等类似意思时：
- 对比维度至少覆盖：架构风格、记忆系统、上下文管理、Skills/Tools、路由、安全、自我改进能力、部署模型。
- 结果写入或更新 `wiki/comparison.md`，登记到 `wiki/INDEX.md`。
- 每个对比项明确标注来源页面，如 `[[hermes-skills.md]]`。

### 4. 知识库巡检
当用户说"检查一下"、"巡检"、"有没有问题"等类似意思时，逐项排查：
- **前后矛盾**: `wiki/` 各页面之间有没有说法打架的地方（比如 A 页说某组件是同步的，B 页说它是异步的）。
- **过期信息**: 有没有安全漏洞的判断已经过时，或者结论需要更新。
- **断链问题**: Skill 迭代记录里有没有引用了已废弃接口却没标注 `[deprecated]` 的地方。

### 5. 时序图
当用户说"画个时序图"、"系统流程"、"组件交互"、"调用链路"等类似意思时：
- **系统级时序**（跨多个组件）→ 新建或更新 `wiki/flows/` 目录下对应文件，文件名用流程名称（如 `message-pipeline.md`）
- **组件内部时序** → 在对应组件页面内新增「时序」章节，不单独建文件
- 时序图统一用 Mermaid `sequenceDiagram` 语法
- 新建 `wiki/flows/` 文件后，同步登记到 `wiki/INDEX.md`
- 优先画 `message-pipeline.md`（消息全链路），这是整个系统的骨架，其他时序图都从它分支出去

### 6. 提问与解答
**每次用户提问或发出请求后**，把这次交互追加到 `wiki/QA.md`，格式如下：
```
## YYYY-MM-DD

### <问题/请求的一句话摘要>
**请求**: <用户原话或近似原话>
**回答概要**: <2-3 句话说清楚做了什么、结论是什么>
**相关页面**: [[wiki页面链接]]（可选）
```
规则：
- **每次都追加，不去重**——保留完整的提问历史，哪怕问题相似
- 同一天的多条记录归在同一个日期标题下
- 回答完之后，主动问用户要不要把结论单独存成 `wiki/insights/YYYY-MM-DD-xxx.md`

## Wiki 页面类型规范

> 每个模块下都有两类页面，命名规则：
> - **开源框架分析页**: `{框架名}-{主题}.md`，如 `hermes-memory.md`
> - **中台设计页**: `{主题}.md`，如 `memory-management.md`

### `wiki/architecture/`

**开源框架分析页**（`hermes-agent.md` / `deerflow.md` / `openclaw.md` 等）
```markdown
# {框架名} {组件/架构} 分析

> 来源: [[raw/xxx]] | 分析日期: YYYY-MM-DD

## 一句话定位
## 核心设计
## 关键实现细节
## 亮点与局限
## 对中台设计的启示
- 参考点: [[architecture/xxx.md]]
```

**中台设计页**（`gateway.md` / `agent-router.md` 等）
```markdown
# {组件名}

## 一句话定位
## 核心设计决策 (ADR)
- **决策** / **背景** / **后果**
## 参考来源
- 参考了 [[hermes-xxx.md]] 的 {具体设计}
- 参考了 [[deerflow-xxx.md]] 的 {具体设计}
## 上下游依赖
- 上游: [[组件A]]  下游: [[组件B]]
## 关键接口/契约
```

### `wiki/security/`

**开源框架分析页**（`hermes-security.md` 等）
```markdown
# {框架名} 安全机制分析
## 已有防御措施
## 已知漏洞/薄弱点
## 对中台安全设计的启示
- 参考点: [[security/agent-security.md]]
```

**中台设计页**（`agent-security.md`）
```markdown
# {安全主题}
## 威胁描述（攻击路径 / 影响范围）
## 防御措施
## 参考来源
## 状态: [ ] 待验证 / [x] 已确认 / ~~已过期~~
```

### `wiki/memory/` 和 `wiki/context/`

**开源框架分析页**（`hermes-memory.md` / `deerflow-memory.md` 等）
```markdown
# {框架名} {记忆/上下文} 机制分析
## 数据结构
## 工作原理
## 局限性与风险
## 对中台设计的启示
- 参考点: [[memory/memory-management.md]]
```

**中台设计页**（`memory-management.md` / `context-management.md`）
```markdown
# {机制名称}
## 作用（解决什么问题）
## 设计方案
## 参考来源
- 采用了 [[hermes-memory.md]] 的 {冻结快照} 机制
- 改进了 [[deerflow-memory.md]] 的 {防抖队列} 策略
## 局限性
```

### `wiki/skills/`

**开源框架分析页**（`hermes-skills.md`）
```markdown
# {框架名} Skill 机制分析
## Skill 定义与格式
## 创建 / 加载 / 激活流程
## 版本与安全机制
## 对中台 Skill 设计的启示
- 参考点: [[skills/skill-iteration.md]]
```

**中台设计页**（`skill-iteration.md`）
```markdown
# {Skill 主题}
## 设计目标
## 继承 / 迭代 / 解析方案
## 参考来源
- 参考了 [[hermes-skills.md]] 的 {渐进式加载} 设计
```

### `wiki/flows/`

系统级时序图，描述跨多组件的调用链路。文件命名：`{流程名}.md`，如 `message-pipeline.md`。

```markdown
# {流程名称} 时序

> 涉及组件: [[组件A]] / [[组件B]] / ...
> 更新日期: YYYY-MM-DD

## 概述
## 时序图
\`\`\`mermaid
sequenceDiagram
    ...
\`\`\`
## 关键节点说明
## 异常路径
```

### `wiki/QA.md` (问答记录)
```markdown
# 知识库 QA 记录

## YYYY-MM-DD

### <问题/请求的一句话摘要>
**请求**: <用户原话或近似原话>
**回答概要**: <2-3 句话>
**相关页面**: [[wiki页面链接]]（可选）
```

### `wiki/log.md` (收录日志)
```markdown
# 收录日志

## <YYYY-MM-DD> - <文件名>
- **来源**: `raw/xxx`
- **新增/更新页面**: [[页面1]]、[[页面2]]
- **新建模块**: 无 / `wiki/新模块/`
```

### `wiki/INDEX.md` (全局索引)

INDEX.md 需要有三个部分：
1. **模块目录表**：每页一行，含模块/路径/页面链接/核心内容摘要（一句话）
2. **模块依赖关系**：文本版架构图，展示各组件的上下游关系
3. **快速入口**：按使用场景分组（了解整体架构 / Agent 内部 / 记忆与上下文 / Skill 体系 / 安全 / 横向对比 / 系统时序）

每次新增 wiki 页面后，必须在 INDEX.md 的模块目录表里追加对应行，并按场景放入快速入口。

## CLAUDE.md 自我优化规则

**每次回答用户问题后**，判断这次对话是否暴露了 CLAUDE.md 的规则空白。满足以下任一条件，就在本次回复末尾说明，并**直接修改 CLAUDE.md**：

| 触发信号 | 说明 | 对应动作 |
|---|---|---|
| 用户纠正了我的做法 | 说明有行为没有被规则约束 | 把正确做法写成新规则 |
| 用户要求了一个没有对应流程的操作 | 如"列功能清单"、"做对比" | 新增对应工作流指令 |
| 用户问题隐含了命名规范、模板或分类方式 | 如"框架分析页"和"中台设计页"的区分 | 补充到对应的页面类型规范 |
| 执行过程中发现现有规则互相矛盾或遗漏 | 如收录流程忘了更新 log.md | 修正对应规则 |

**不需要修改 CLAUDE.md 的情况**：用户在问具体知识点、做 wiki 内容更新、日常提问——这些不属于行为规范层面的变化。

**修改时的原则**：
- 只修改工作流指令和规范章节，不改项目概览和三层架构描述。
- 新规则用口语化表达（同现有风格），不写技术黑话。
- 修改完后告知用户"已更新 CLAUDE.md：新增了 XX 规则"。

## 回复风格要求

- **语言**: 全程中文，专有名词保留英文（如 Agent、ADR、Skill）。
- **长度**: 能用一句话说清楚的不写两句；操作步骤用编号列表。
- **态度**: 直接给结论，不做不必要的铺垫或总结性废话。
- **不确定时**: 明确说"我不确定"，不要编造。

## 技术问题回答规范

用户提技术问题时，**先判断他真正想了解的是什么**，不要停留在"描述这个东西是什么"。

这个知识库的关注领域是 AI 系统架构，用户提问的背后通常是以下几种意图：

| 问法特征 | 背后真实意图 | 回答重心 |
|---|---|---|
| "X 是什么"、"X 怎么工作" | 想理解设计思路和目的，不只是功能描述 | 为什么这么设计、解决了什么问题、适用什么场景 |
| "为什么这么做" | 想理解约束条件和权衡 | 工程约束 / 备选方案 / 代价是什么 |
| "X 和 Y 的区别" | 想选型或理解适用边界 | 核心差异 + 各自适合什么场景 |
| "X 有什么优点" | 想知道设计收益，通常也想知道代价 | 优点背后的设计动机 + 对应的局限 |

**默认行为**：技术特性问题，优先解释"**为什么这么设计**"和"**解决了什么场景问题**"，然后才是实现细节。实现细节只在用户追问或明确需要时展开。

**"怎么做"类问题的特殊规则**：用户问"怎么实现"、"怎么做"、"如何配置"、"步骤是什么"时，**严格依据项目内容作答**，不得凭通用知识推断或编造。查找顺序如下：

1. **优先查 `wiki/`**：有明确描述 → 引用对应页面直接回答
2. **wiki 没有，查 `raw/`**：找到相关内容 → 先把内容补充到对应 wiki 页面，再基于补充后的 wiki 回答，并告知用户"已从 raw 补充到 wiki"
3. **wiki 和 raw 都没有** → 明确回答"**项目文档中未找到相关内容**"，说明查过哪些页面，建议用户补充原始资料